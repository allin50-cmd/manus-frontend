name: Deploy Production Stack

on:
  push:
    branches: [main, master]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'aios'
        type: string

env:
  ENVIRONMENT_NAME: ${{ github.event.inputs.environment || 'aios' }}
  RESOURCE_GROUP: rg-aios

jobs:
  # ============================================================
  # Build & push Docker image to ACR
  # ============================================================
  build:
    name: Build & Push Image
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.version }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Azure Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.ACR_LOGIN_SERVER }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.ACR_LOGIN_SERVER }}/vaultline-api
          tags: |
            type=sha,format=short
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ============================================================
  # Deploy infrastructure via Bicep
  # ============================================================
  infra:
    name: Deploy Infrastructure
    runs-on: ubuntu-latest
    needs: build

    steps:
      - uses: actions/checkout@v4

      - name: Log in to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Ensure resource group exists
        run: |
          az group create \
            --name ${{ env.RESOURCE_GROUP }} \
            --location uksouth \
            --output none

      - name: Deploy Bicep
        uses: azure/arm-deploy@v2
        with:
          subscriptionId: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          resourceGroupName: ${{ env.RESOURCE_GROUP }}
          template: ./infra/main.bicep
          parameters: >
            environmentName=${{ env.ENVIRONMENT_NAME }}
            acrLoginServer=${{ secrets.ACR_LOGIN_SERVER }}
            dbPassword=${{ secrets.DB_PASSWORD }}
            openaiKey=${{ secrets.OPENAI_API_KEY }}
            resendKey=${{ secrets.RESEND_KEY }}
            stripeSecretKey=${{ secrets.STRIPE_SECRET_KEY }}
          failOnStdErr: false

  # ============================================================
  # Update Container App with new image
  # ============================================================
  deploy:
    name: Deploy to Container App
    runs-on: ubuntu-latest
    needs: [build, infra]
    environment: production

    steps:
      - name: Log in to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Update Container App image
        run: |
          az containerapp update \
            --name api-${{ env.ENVIRONMENT_NAME }} \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --image ${{ secrets.ACR_LOGIN_SERVER }}/vaultline-api:sha-${{ github.sha }} \
            --output none

      - name: Wait for revision to stabilize
        run: |
          az containerapp revision list \
            --name api-${{ env.ENVIRONMENT_NAME }} \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --query "[0].properties.healthState" \
            --output tsv

      - name: Record deployment
        run: |
          API_URL=$(az containerapp show \
            --name api-${{ env.ENVIRONMENT_NAME }} \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --query "properties.configuration.ingress.fqdn" \
            --output tsv)

          curl -s -X POST "https://${API_URL}/api/deployments/record" \
            -H "Content-Type: application/json" \
            -H "X-DEPLOY-TOKEN: ${{ secrets.DEPLOY_RECORD_TOKEN }}" \
            -d '{
              "environment": "prod",
              "status": "success",
              "commit": "${{ github.sha }}",
              "workflowRun": "${{ github.run_id }}"
            }' || true
