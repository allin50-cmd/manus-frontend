#!/usr/bin/env node
'use strict';
const fs=require('fs'),path=require('path');const resource=process.argv[2];if(!resource){console.error('Usage: node inspect-fields.js <resource>');process.exit(1)}const file=path.join(__dirname,'export',`${resource}.json`);if(!fs.existsSync(file)){console.error(`Not found: ${file}`);process.exit(1)}const rows=JSON.parse(fs.readFileSync(file,'utf8'));const out={};for(const row of rows.slice(0,500)){if(!row||typeof row!=='object'||Array.isArray(row))continue;for(const[k,v]of Object.entries(row)){const t=v===null?'null':Array.isArray(v)?'array':typeof v;out[k]??={types:{},present:0,examples:[]};out[k].types[t]=(out[k].types[t]||0)+1;out[k].present++;if(out[k].examples.length<3&&v!==null&&v!=='')out[k].examples.push(v)}}console.log(JSON.stringify({resource,recordCount:rows.length,fields:out},null,2));
